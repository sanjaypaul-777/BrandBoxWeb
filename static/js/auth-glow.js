/**
 * auth-glow.js — Logo glow is CSS-only (static).
 * Kept as a no-op so existing template script tags stay valid.
 * Animated ripples/blur were removed — they triggered empty black
 * Chromium autofill popups when focusing username/password.
 */
(function () {
  "use strict";
})();
